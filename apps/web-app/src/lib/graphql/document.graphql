mutation UploadDocument($input: DocumentInput!) {
  uploadDocument(input: $input) {
    id
    fileName
    status
    retentionPolicy
    storagePath
    purgedAt
    createdAt
    updatedAt
    fileMetadata {
      mimeType
      sizeInKb
    }
  }
}
