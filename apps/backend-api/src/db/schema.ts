// Re-export database schema from the shared database package
export {
  users,
  fiscalEntities,
  financialEntries,
  documents,
  userOnboarding,
  feedback,
  feedbackCategoryEnum,
} from '@hq/database';
