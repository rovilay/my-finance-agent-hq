# name: Deploy to Production

# on:
#   push:
#     branches: [main]
#   workflow_dispatch:

# env:
#   REGISTRY: ghcr.io
#   BACKEND_IMAGE_NAME: ${{ github.repository }}-backend
#   FRONTEND_IMAGE_NAME: ${{ github.repository }}-frontend

# jobs:
#   build-and-push-backend:
#     name: Build & Push Backend Image
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Extract metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE_NAME }}
#           tags: |
#             type=ref,event=branch
#             type=sha,prefix=prod-
#             type=semver,pattern={{version}}
#             type=raw,value=production-latest
#             type=raw,value=latest

#       - name: Build and push backend image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./apps/backend-api/Dockerfile
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#   build-and-push-frontend:
#     name: Build & Push Frontend Image
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Extract metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE_NAME }}
#           tags: |
#             type=ref,event=branch
#             type=sha,prefix=prod-
#             type=semver,pattern={{version}}
#             type=raw,value=production-latest
#             type=raw,value=latest

#       - name: Build and push frontend image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./apps/web-app/Dockerfile
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           build-args: |
#             NEXT_PUBLIC_API_URL=${{ secrets.PRODUCTION_API_URL }}

#   deploy-production:
#     name: Deploy to Production Environment
#     runs-on: ubuntu-latest
#     needs: [build-and-push-backend, build-and-push-frontend]
#     environment:
#       name: production
#       url: https://your-domain.com

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Deploy to production
#         run: |
#           echo "🚀 Deploying to production environment..."
#           echo "Backend image: ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE_NAME }}:production-latest"
#           echo "Frontend image: ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE_NAME }}:production-latest"
#           # Add your deployment commands here

#       - name: Run database migrations
#         run: |
#           echo "Running database migrations..."
#           # Add migration commands here

#       - name: Verify deployment
#         run: |
#           echo "Verifying deployment..."
#           # Add health check commands here

#       - name: Create deployment tag
#         run: |
#           DATE=$(date +%Y%m%d-%H%M%S)
#           echo "Creating deployment tag: prod-$DATE"
#           # git tag prod-$DATE
#           # git push origin prod-$DATE

#       - name: Notify success
#         if: success()
#         run: |
#           echo "✅ Production deployment successful!"
#           # Add notification logic
